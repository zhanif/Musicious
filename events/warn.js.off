const { Client } = require("discord.js")

module.exports = {
    name: 'warn',
    /**
     * @param {Client} client
     */
    run: async(client, error) => {
        return console.log(`Warn: ${error}`)
    }
}
